// Minimal stub to satisfy TypeScript when resolving the implicit "google-cloud" type library.
// The project uses scoped @google-cloud packages, so we don't need any ambient globals here.
export {};
