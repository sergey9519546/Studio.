// Add Comment model for real-time moodboard comments
model Comment {
  id             String        @id @default(uuid())
  moodboardItemId String       @map("moodboard_item_id")
  userId         String        @map("user_id")
  userName       String        @map("user_name")
  text           String
  createdAt      DateTime      @default(now()) @map("created_at")
  updatedAt      DateTime      @updatedAt @map("updated_at")

  @@index([moodboardItemId])
  @@index([userId])
  @@map("comments")
}

// Add this model to your existing schema.prisma after MoodboardItem model
